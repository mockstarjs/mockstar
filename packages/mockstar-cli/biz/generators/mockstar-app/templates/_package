{
  "name": "<%= name %>",
  "scripts": {
    "start": "mockstar start",
    "stop": "mockstar stop",
    "logs": "mockstar pm2 logs"
  },
  "dependencies": {
    "mockstar": "^<%= pkgVersion['mockstar'] %>"
  }
}
