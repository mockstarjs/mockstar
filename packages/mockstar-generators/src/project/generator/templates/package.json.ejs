{
  "name": "<%= projectConfig.name %>",
  "private": true,
  "scripts": {
    "start": "mockstar start --watch",
    "dev": "mockstar run --dev --watch",
    "stop": "mockstar stop"
  },
  "dependencies": {
    "mockstar-cli": "^<%= projectConfig.pkgVersion['mockstar'] %>"
  },
  "devDependencies": {
    "mockstar-cli": "^<%= projectConfig.pkgVersion['mockstar'] %>"
  }
}
