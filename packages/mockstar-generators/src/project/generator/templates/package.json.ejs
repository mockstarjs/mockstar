{
  "name": "<%= businessProject.name %>",
  "private": true,
  "scripts": {
    "start": "mockstar start --watch",
    "dev": "mockstar run --dev --watch",
    "stop": "mockstar stop"
  },
  "dependencies": {
    "mockstar": "<%= businessProject.pkgVersion['mockstar'] %>"
  },
  "devDependencies": {
    "mockstar-cli": "<%= businessProject.pkgVersion['mockstar-cli'] %>"
  }
}
