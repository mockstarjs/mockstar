{
  "name": "mockstar-app-9527",
  "private": true,
  "scripts": {
    "start": "mockstar start --watch",
    "dev": "mockstar run --dev --watch",
    "stop": "mockstar stop"
  },
  "dependencies": {
    "mockstar-cli": "^1.2.2"
  },
  "devDependencies": {
    "mockstar-cli": "^1.2.2"
  }
}
