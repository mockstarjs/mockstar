{
  "name": "mockstar-app-9420",
  "private": true,
  "scripts": {
    "start": "mockstar start --watch",
    "dev": "mockstar run --dev --watch",
    "stop": "mockstar stop"
  },
  "dependencies": {
    "mockstar": "^1.3.7"
  },
  "devDependencies": {
    "mockstar-cli": "^1.3.19"
  }
}
